{
  "framework": "vite",
  "installCommand": "npm install",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/orcamento/:id", "destination": "/index.html" }
  ]
}